{
  "type": "EXPRESSION",
  "group": "A.7 Physical controls",
  "control": "A.7.7",
  "name": "Screen lock requires a password",
  "description": "Clear desk and clear screen. A screensaver that blanks the display without asking for a password protects nothing.",
  "query": "SELECT enabled, grace_period FROM screenlock;",
  "expression": "results.size() > 0 && results[0].enabled == '1' && results[0].grace_period <= 300"
}
