{
  "type": "EXPRESSION",
  "group": "A.12 Operations Security",
  "name": "Automatic updates enabled",
  "description": "macOS does not write this key while the setting is on, which is the default, so a missing key passes.",
  "query": "SELECT value FROM plist WHERE path = '/Library/Preferences/com.apple.SoftwareUpdate.plist' AND key = 'AutomaticCheckEnabled';",
  "expression": "results.size() == 0 || results[0].value == '1'"
}
