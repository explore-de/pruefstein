{
  "type": "EXPRESSION",
  "group": "A.8 Technological controls",
  "control": "A.8.7",
  "name": "System Integrity Protection enabled",
  "description": "Protection against malware.",
  "query": "SELECT enabled FROM sip_config WHERE config_flag = 'sip';",
  "expression": "results.size() > 0 && results[0].enabled == '1'"
}
