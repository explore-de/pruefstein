{
  "type": "EXPRESSION",
  "group": "A.8 Technological controls",
  "control": "A.8.20",
  "name": "Remote login (SSH) disabled",
  "description": "Networks security: every inbound sharing service is attack surface on an endpoint that need not serve anything.",
  "query": "SELECT remote_login FROM sharing_preferences;",
  "expression": "results.size() > 0 && results[0].remote_login == '0'"
}
