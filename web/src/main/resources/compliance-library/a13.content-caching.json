{
  "type": "EXPRESSION",
  "group": "A.13 Communications Security",
  "name": "Content caching disabled",
  "description": "A.13.1 — turns the endpoint into a download cache serving other devices on the network (Richtlinie zum sicheren IT-Betrieb: Minimalbetrieb).",
  "query": "SELECT content_caching FROM sharing_preferences;",
  "expression": "results.size() > 0 && results[0].content_caching == '0'"
}
